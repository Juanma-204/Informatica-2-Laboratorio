#ifndef CONSTANTES_H
#define CONSTANTES_H

// Dimensiones del área de simulación
const double WIDTH = 800.0;
const double HEIGHT = 600.0;

// Parámetros de movimiento
const double VELOCIDAD_MIN = -2.0;
const double VELOCIDAD_MAX = 2.0;

// Número de pasos de simulación
const int PASOS_SIMULACION = 500;

// Tamaño del paso (tiempo simulado)
const double DT = 1.0;

#endif
